# 简单的Nginx测试容器
FROM nginx:alpine

# 创建dist目录
RUN mkdir -p /dist

# 复制测试文件
COPY nginx-test.conf /etc/nginx/conf.d/default.conf

# 创建一个简单的测试页面
RUN echo "<html><body><h1>Nginx Test Page</h1><p>If you see this, Nginx is working!</p></body></html>" > /dist/index.html

# 暴露端口
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]