# 后端生产Dockerfile - 简化版本（与现有部署一致）
FROM node:22-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./
RUN npm ci

# 复制源代码
COPY . .

# 创建必要的目录
RUN mkdir -p /app/data /app/uploads

EXPOSE 3001

# 使用tsx运行TypeScript文件（与现有部署一致）
CMD ["npx", "tsx", "api/server.ts"]